#!/usr/bin/env ruby

require 'ramaze'

runner = ARGV.find{|file| File.file?(file)} || 'main.rb'

begin
	puts "running #{File.expand_path(runner)}"
  require runner
  Ramaze.start
rescue LoadError => ex
  puts ex
end
