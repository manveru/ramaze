<html>
  <head>
    <title>Template::Ezamar external</title>
  </head>
  <body>
  <h1>The external Template for Ezamar</h1>
    #{link :/, :title => 'Home'}
    <p>
      Here you can pass some stuff if you like, parameters are just passed like this:<br />
      #{link Rs(:external, :one), :title => "external/one"}<br />
      #{link Rs(:external, :one, :two, :three), :title => "external/one/two/three"}<br />
      #{link Rs(:external, :one, :foo => :bar), :title => "external?foo=bar"}<br />
    </p>
    <div>
      *args
      <?r @args.each do |arg| ?>
        <span>#{arg}</span>
      <?r end ?>
    </div>
    <div>
      #{request.params.inspect}
    </div>
  </body>
</html>
